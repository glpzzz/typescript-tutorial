// removing the types restriction with any
var age = 25;
console.log(age);
age = '1234';
console.log(age);
age = true;
console.log(age);
// just to use in rare cases.
// using in arrays
var mixed = [];
mixed.push(1);
mixed.push('1');
mixed.push(true);
// using in objects
var ninja;
